# TEMPLATE ONLY — placeholders, NOT real values. Do NOT `kubectl apply` this and
# do NOT commit a filled-in copy. Fill values locally, then seal:
#   make seal-secret NS=app-dev IN=./app-back-app.yaml OUT=secrets/app-dev-app-back-app.yaml
apiVersion: v1
kind: Secret
metadata:
  name: app-back-app
  namespace: app-dev            # repeat for app-prod with prod values
type: Opaque
stringData:
  APP_KEY: "base64:REPLACE_ME"          # php artisan key:generate --show
  DB_USERNAME: "app-back"
  DB_PASSWORD: "REPLACE_ME"             # openssl rand -base64 24
  BACKUP_DB_USER: "backup"
  BACKUP_DB_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  AWS_ACCESS_KEY_ID: "REPLACE_ME"       # SeaweedFS app-files identity
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  MAIL_MAILER: "smtp"
  MAIL_HOST: "REPLACE_ME"
  MAIL_USERNAME: "REPLACE_ME"
  MAIL_PASSWORD: "REPLACE_ME"
